name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
      working-directory: SemgusParser
    - name: Build
      run: dotnet build --no-restore --configuration Release
      working-directory: SemgusParser
    - name: Publish (Linux)
      run: dotnet publish -c Release -o ../output/linux -r linux-x64 --self-contained true
      working-directory: SemgusParser
    - name: Publish (Windows)
      run: dotnet publish -c Release -o ../output/win -r win-x64 --self-contained true
      working-directory: SemgusParser
    - name: Publish (macOS)
      run: dotnet publish -c Release -o ../build/osx -r osx-x64 --self-contained true
      working-directory: SemgusParser
    - name: Upload Output
      uses: actions/upload-artifact@v2
      with:
        name: semgus-parser
        path: output
