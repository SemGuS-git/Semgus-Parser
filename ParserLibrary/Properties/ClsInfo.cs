using System;

// The ANTLR-generated files are not CLSCompliant, so we mark the whole assembly as such.
// If we need to be CLSCompliant, the generated files need to be in their own assembly.
[assembly:CLSCompliant(false)]
namespace Semgus.Parser
{
}
